import { AnimationsObjectsDirective } from './animations-objects.directive';

describe('AnimationsObjectsDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationsObjectsDirective();
    expect(directive).toBeTruthy();
  });
});
